The review is complete and the plan is written to `/Users/kenneth/.claude/plans/fable-review-packet-inherited-twilight.md`. (ExitPlanMode is not available in this session's toolset — I searched for it — so I'm closing with the ruling directly; the plan file is in place for approval.)

## Ruling: **Approve after listed fixes** — Option B is sound

I inspected the named implementation at `5cf870a` (not just the docs): the core knowledge model, projection, and concurrency handling are solid — open concept types, lossless property preservation, evidence-carrying relations, stale-epoch rejection in `graphConceptDetail`, careful generation guards in `WorkspaceGraph.refreshFile`, a content-independent layout seed, and **zero public-contract changes** in the diff (verified: only a CLI version bump touches `packages/cli`; `command-protocol.ts` unchanged). No trust-boundary, persistence, or data-loss defect.

However, I found **three confirmed defects in exactly the coherence surface Option B is being judged on**, all cheap and bounded:

1. **Backlink sources never appear in the local neighborhood** (`workspace-graph.ts:409` keeps the backlink's `note` pointed at the *target*, so `contextForNote`'s neighborhood structurally excludes linking notes). A backlink-only note shows "Linked from" entries in the Links tab and "No neighborhood yet" in the adjacent Graph tab. The renderer test fixture (`App.test.tsx:1102`) hand-writes the intended contract and masks this; the unused legacy `graph-query` path defines it correctly.
2. **"Expand to full graph" passes a MouseEvent as `focusPath`** (`GraphNeighborhoodView.tsx:62` `onClick={onOpenCanvas}` → `openGraphCanvas(event)`), so the focus contract silently no-ops and an event object lands in state.
3. **Refresh with an unchanged graph scrambles the layout**: `SpatialGraphView` resets positions to the raw seed on every reload, but the layout worker is keyed on `sourceSnapshotId` and never re-runs — contradicting commit `146298a`'s "preserve layout" claim.

On the specific questions: the **Node 26 CLI p50 miss is an acceptable documented process-start exception** (a ~100 ms runtime floor, not an Exo regression — split the budget into per-runtime process-start floor vs. Exo work, keep EXO-ISSUE-111 open for the native-launcher decision, don't silently weaken the 99 ms in-app number, which is green). **GraphBench stays** (38 tracked files, isolated workspace package, opt-in scripts, no product imports, not in `ci:check`). The **profile foundation correctly leaves room for user-owned ontologies** (one post-main nit: the unknown-profile fallback drops the requested id). The **QMD patch is fine** under the documented EXO-ISSUE-117 exception. The **shared inspected-Concept model stays post-main** — fixes 1–2 cover the contradictory-UI cases.

Pre-main gate: fixes 1–3, the packaged graph journey E2E, the CLI budget split with evidence, issue-tracker/CHANGELOG bookkeeping, then `pnpm ci:check` + `stable:smoke` + `test:e2e` + `pack:mac` from the exact fixed tree. Everything else (compact 10K IPC, Stellar integration, profile authoring, publication matrix, maintenance Skill, legacy graph-query consolidation) is explicitly named post-main `dev` work in the plan so the gate can't silently expand.
