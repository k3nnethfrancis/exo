import { test } from "@playwright/test";

test("finds and opens a note through the explorer search journey", async () => {
  test.fixme(
    true,
    "U2: SidebarSearchPane is implemented, but the current explorer renders no control that can switch from files to search.",
  );
});
