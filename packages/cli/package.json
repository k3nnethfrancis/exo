{
  "name": "@exograph/cli",
  "version": "0.1.0-alpha.3",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node ../../scripts/build-cli.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:unused": "tsc -p tsconfig.production.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@exograph/core": "workspace:*"
  }
}
